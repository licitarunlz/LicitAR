@{
    ViewData["Title"] = "Error de Conexión";
    Layout = "~/Views/Shared/_BlankLayout.cshtml";
}

<div class="container-xxl">
  <div class="authentication-wrapper authentication-basic container-p-y">
    <div class="authentication-inner">
      <div class="card px-sm-6 px-0">
        <div class="card-body">
          <div class="app-brand justify-content-center mb-6">
            <a href="/" class="app-brand-link gap-2">
              <span class="app-brand-logo demo">@await Html.PartialAsync("../_Partials/_Macros")</span>
              <span class="app-brand-text demo text-heading fw-bold">@TempData.Peek("appName")</span>
            </a>
          </div>
          <div class="text-center mb-4">
            <i class="bx bx-plug-off" style="font-size: 3rem; color: #ff4d4f;"></i>
          </div>
          <h4 class="mb-2 text-center">No se pudo conectar a la base de datos</h4>
          <p class="mb-4 text-center">
            Lo sentimos, el sistema no pudo establecer conexión con la base de datos.<br>
            Por favor, intente nuevamente más tarde o contacte al administrador si el problema persiste.
          </p>
          <div class="text-center mt-4">
            <a href="~/Usuario/Login" class="btn btn-primary">
              <i class="bx bx-refresh me-1"></i> Reintentar
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
